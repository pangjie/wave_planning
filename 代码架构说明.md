# 波次自动化工具 · 代码架构说明（AI 阅读版）

> 本文档面向需要接手、审查或修改本项目的 AI / 开发者：按“系统总览 → 前端 → 后端 → 联动协议 → 安全不变量 → 测试 → 扩展”顺序组织。
> 配套文件：`README.md`（部署/使用说明）。

---

## 1. 系统总览

本系统由两个子系统深度整合为一个本地控制台：

```text
波次规划工具（单文件 HTML）
  · 订单导入 → 渠道/分段规划 → Excel 导出
  · 任务提交 / 轮询 / 波次号回填 / 主题外观
        │   HTTP（仅本机 127.0.0.1:8000）
        ▼
WMS 自动化后端（FastAPI + Playwright）
  · JobManager .......... 内存任务队列（单任务并发）
  · WmsExportAutomation ......... 订单导出（全量分析）
  · WmsWaveGenerateAutomation ... 生成波次（核心工作流）
  · WmsWavePrintAutomation ...... 打印波次
  · WmsWavePickAutomation ....... 批量拣货
        │   系统 Chrome（持久化会话，复用 WMS 登录）
        ▼
领星 WMS：https://wms.xlwms.com
```
关键事实：

- 前端是**无构建依赖的单文件 HTML**：`planner/` 下的 `part0-head.html`、`part1-body.html`、`part2-fonts.html`、`core.js`、`app.js`、`vendor/xlsx.full.min.js` 由 `planner/build.py` 组装为顶层 `波次规划工具.html`，并同时部署为 `wms/project/frontend/dist/index.html`（后端托管的控制台页面）。
- 后端用 Playwright 打开**系统 Chrome**（`channel: "chrome"`）+ **持久化 user-data-dir**（`wms/project/data/browser-profile/`），从而复用用户在 Chrome 中完成的 WMS 登录。
- 任务对象只存在**内存**（JobManager），重启进程即丢失；波次号等少量结果通过 `data/wave-records.json` 持久化。
- 生产安全是第一优先级：见 §7。

---

## 2. 顶层目录

```text
波次自动化工具/
├── 波次规划工具.html        # 前端构建产物（单文件）
├── README.md                # 部署/使用说明
├── 代码架构说明.md           # 本文档
├── sample/                  # 测试样本订单（5700 单）
├── planner/                 # 前端源码与测试
│   ├── build.py             # 组装单文件页 + 部署 dist
│   ├── part0-head.html      # CSS（变量/主题/布局）
│   ├── part1-body.html      # DOM 结构
│   ├── part2-fonts.html     # @font-face 内嵌字体
│   ├── core.js              # 领域逻辑（纯 JS，无 DOM）
│   ├── app.js               # 交互逻辑（DOM/状态/WMS 联动）
│   ├── vendor/xlsx.full.min.js  # SheetJS 只读解析
│   ├── fonts/               # woff2 字体源
│   ├── test-helpers.js      # 测试基建（Chrome/CDP/样本导入）
│   ├── mock-wms-server.js   # 8000 端口假后端（UI 测试用）
│   └── test-*.js            # 测试套件（见 §8）
└── wms/project/             # 后端服务根目录
    ├── backend/app/
    │   ├── main.py          # FastAPI 组装 + 静态托管
    │   ├── core/config.py   # Settings + AutomationConfig
    │   ├── api/routes.py    # 全部 HTTP 接口
    │   ├── automation/      # Playwright 自动化（5 个模块）
    │   └── services/        # job_manager、wave_records
    ├── config/automation.json  # 选择器/超时/工作流配置
    ├── frontend/dist/       # 托管页面（构建产物）
    ├── scripts/             # setup/start/dev/驱动/冒烟
    └── tests/               # 后端 pytest
```

---

## 3. 前端（planner/）

### 3.1 构建管线 build.py

1. 读取 `part0-head.html`，把 `part2-fonts.html` 的内容注入 `</head>` 前（内嵌字体）；
2. 拼接 `part1-body.html`；
3. 把 `core.js`、`app.js`、`vendor/xlsx.full.min.js` 依次作为 `<script>` 内联；
4. 写出顶层 `波次规划工具.html`，并拷贝为 `wms/project/frontend/dist/index.html`。

**结论**：改前端只改 `planner/` 下的 part/core/app 文件，然后运行 `python3 build.py`；服务静态文件按请求实时读盘，改页面无需重启后端。

### 3.2 core.js —— 领域逻辑（纯函数，不碰 DOM）

自上而下的处理管线：

1. **导入解析**
   - `locateSheet(wb)`：找“出库单”工作表；
   - `mapHeaders`：表头归一化（去空白、小写），识别必需列与 `SKU n SKU` 多列；
   - `buildRecords`：逐行生成订单记录，**同一出库单号跨多行时合并为一条记录**（`byOrder` Map，SKU 去重、首个非空 SKU 作 `sku1`）；全空行跳过；空出库单号生成 `第N行` 占位名；
   - `trackingChannel(tracking)`：按跟踪号前缀/长度规则识别物流渠道，不匹配归 `未识别`；CBT/CBS 按承运商列直接归类。
2. **记录三分类**：`单品单件 / 单品多件 / 多品混合`，其余任何未知类型一律进混件池（**绝不丢单**）。
3. **渠道内分段** `analyzeChannel`：
   - 单品单件：按 SKU 聚组 → `hotSeq`（count ≥ hotLine）与 `paperSeq`，各用 `partitionByCapacity`（容量背包）切段，段名 `爆品N` / `单件paper-N`；
   - 单品多件：`linearPartition`（连续均衡 DP）切 `多件N`；
   - 混件：`buildComponents`（按共享 SKU 并查集成组件）→ `discreteAllocate`（离散分配到 mixSegs 个 slot）→ `slotsToMixSegs`，段名 `混件N`；`absorb` 四档决定多件/paper 是否被吸收或全量重建进混件池；
   - 每个分段 `finalizeSeg`：orderNos（去重）、skuPool、orderCount、pickQty（qtyMap 求和）。
4. **全量分析** `analyze(records, qtyMap, mode, paramsFor)`：按渠道（拆分=channelId；归并=CBT/CBS/普通）分组 → 逐渠道 analyzeChannel → `sortByDefault`（未识别垫底，按总量降序）→ 汇总 totals/waveStats/classSeq。
5. **导出** `buildExport(analysis, exportState)` → 8 个 sheet（见 §3.3），`collectBlocks` 只收**选中渠道 ∩ 选中分段**。
6. **OOXML 写出**：自建 SpreadsheetML + STORE 压缩（`zipStore` + crc32），产出 xlsx 字节；`downloadWorkbook` 触发浏览器下载。

**订单分配不变量（必须恒成立）**：全部渠道的 `ch.total` 之和 = 记录总数；每渠道内所有分段的 orderNos 并集 = `ch.total`；任何订单恰好属于一个分段。测试 `test-invariants.js` 强制验证（含勾选、归并、参数调整后）。

### 3.3 导出的 8 个工作表

| Sheet | 生成函数 | 内容 |
| --- | --- | --- |
| 分组结果 | `sheetGroupResult` | 每分段一列：渠道名行 + 分段信息行 + “start…end”一键复制块 + 逐单列出行 |
| 分组结果SKU池 | `sheetGroupSkuPool` | 每分段 SKU/数量两列 |
| 波次表 | `sheetWaveTable(blocks, waveNos)` | 每分段一行；**第 3 列（索引 2）写波次号**，键 `channelId|segName` |
| 渠道统计 | `sheetChannelStats` | 各渠道订单/参数/分段汇总 |
| 单件/多件/混件SKU序列 | `sheetSingleSeq` 等 | 选中分段内的 SKU 序列（含被吸收 SKU 的补全 `mixSeqExtra`） |
| 分类SKU序列 | `sheetClassSeq` | 固定口径：普通/CBT/CBS × 单品单件 SKU 频次 |

### 3.4 app.js —— 状态与交互

核心状态 `state`：

```js
{
  fileName, records, qtyMap, merged,
  modes: { normal: { sel:{channels,segs,seenChannels,seenSegs}, per, unified, order }, merged: {...} },
  analysis: { normal, merged },   // 缓存，参数变更时 invalidate
  waveNos: Map,                   // 'channelId|segName' -> 波次号（本批会话内）
}
```

- **选中语义**：`segEffectiveOn(chId, segName)` = 渠道选中 && 分段选中；`syncSelection` 在每次分析后把新出现的渠道/分段默认选中；参数变更（`setUnified/setChannelParam/cycleAbsorb`）→ `invalidate` + 重算 + **清空波次历史**（`wmsClearWaveHistory`，因为分段重排后旧波次号不再对应）。
- **渲染**：`renderDash` 生成表格；`refreshSegLine`/`refreshChnRow` 就地更新勾选/变绿状态；`waved` 类仅在**分段有效且有波次号**时显示。
- **主题**：`applyTheme(v)` 只增删 `body[data-theme=...]` 属性 → CSS 变量换色；**不触碰任何状态**（运行中任务不受影响）。主题色板定义在 part0-head 的 `body[data-theme=...]` 块，含表格写死底色的覆盖。
- **WMS 联动（app.js 后半段）**：见 §5 协议细节；状态对象 `wms = { available, job, jobId, timer, events, active, mode, headless, clearPending, linkedExports, ... }`。

---

## 4. 后端（wms/project/backend/）

### 4.1 配置模型 core/config.py

- `Settings.from_environment()`：所有路径/开关均可用环境变量覆盖（`WMS_AUTOMATION_CONFIG`、`WMS_BROWSER_PROFILE`、`WMS_DOWNLOADS_DIR`、`WMS_OUTPUTS_DIR`、`WMS_HEADLESS`、`WMS_BROWSER_CHANNEL`、`WMS_PROJECT_ROOT`）。
- `AutomationConfig`（由 `config/automation.json` 校验）：顶层导出配置 + `wave_picking` / `wave_printing` / `wave_generation` 三个工作流子配置，各自含 `target_url`、`selectors`、`timeouts_ms` 等。
- `wave_generation` 特有：`multi_search_max_lines`（≤700）、`page_size_option`（1000条/页）、`wave_no_pattern`、`search_verify_wait_ms`（搜索数量核对短窗等待，默认 4000ms）。

### 4.2 HTTP 接口（api/routes.py）

| 方法/路径 | 用途 | 关键点 |
| --- | --- | --- |
| GET `/api/health` | 健康检查 | 前端据此显示/隐藏 WMS 区 |
| POST `/api/jobs` | 提交任务 | `CreateJobRequest`：mode、browser_mode、confirm_production（必填 true）、wave_nos / segments；对出库单号/波次号做 `WAVE_NO_PATTERN` 白名单校验与去重；活动任务冲突返回 409 |
| GET `/api/jobs` | 任务列表 | 最近 50 条，倒序 |
| GET `/api/jobs/{id}` | 单任务（含 events 事件流） | 前端每秒轮询 |
| POST `/api/jobs/{id}/cancel` | 取消任务 | 触发 asyncio.CancelledError → 关闭浏览器 |
| GET `/api/jobs/{id}/file` | 下载导出订单文件 | 目录白名单 |
| GET `/api/jobs/{id}/merged` | 下载合并 PDF | 打印工作流 |
| POST `/api/exports?filename=` | 保存前端生成的波次规划 Excel | 校验 `PK\x03\x04` 魔数，输出目录内自动加序号后缀防覆盖 |
| GET `/api/exports` | 最近 1 个导出文件清单 | 前端刷新后恢复下载链接 |
| GET `/api/exports/{name}` | 下载导出文件 | 白名单（限 outputs 目录） |
| GET `/api/wave-records` | 已生成波次记录列表 | 前端 5 秒同步回填变绿 |
| POST `/api/wave-records/clear` | 清空波次记录 | 导入新订单文件时调用 |

### 4.3 任务生命周期 JobManager（services/job_manager.py）

- 内存 `jobs: dict[id, JobRecord]` + `tasks: dict[id, Task]`；`_run_lock` 保证**同一时刻只跑一个任务**；
- `create()` 拒绝并发（ActiveJobConflictError → 409）；
- `_execute()`：按 mode 调用对应自动化 `run(progress, headless=..., segments/wave_nos=...)`；`progress(stage, message)` 回调把事件追加进 `job.events`（状态置 running）；
- **生成波次逐段持久化**：progress 收到 `segment_wave` 事件（JSON：channel/seg_name/wave_no）时立即 `upsert_records` 写 `data/wave-records.json`——即使进程崩溃，已完成分段的波次号也不丢；
- 结束后按 `failed_wave_nos / failed_count` 判定 `succeeded / partial`，异常 → `failed`；取消走 `CancelledError` 分支 → `cancelled`。

### 4.4 自动化模块（automation/）

公共设施 `common.py`：`AutomationError`（可安全展示给用户的失败）、`WAVE_NO_PATTERN`、`open_browser_context`（持久化 profile + 可选下载目录）、`first_page`、`wait_for_loading`（loading 遮罩消失封装）。

**WmsWaveGenerateAutomation（wms_wave_generate.py，最核心）**：

`run()` 主循环：
1. 打开页面 → `_wait_for_page_ready`（页签/搜索栏出现 + 遮罩消失）→ `_ensure_no_blocking_dialog`（开工前有意外弹窗即停）→ `_select_pending_tab`（唯一性校验后点击“待处理”）→ `_set_page_size`（**先检查**当前是否已是 1000条/页，是则跳过）；
2. 逐段 `_generate_one`，段内步骤：
   - 定位出库单号搜索栏（`:has(span.fake-select-label:text-is("出库单号"))`，页面有多个搜索栏，必须精确定位）；输入框可见且非禁用；
   - 打开“多项精确搜索”弹层（先探测是否已开，避免开合反转）→ 粘贴一行一个出库单号（每块 ≤ 700 行）→ 点搜索 → 等遮罩消失；
   - `_verify_search_total`：读取子页签“待处理/已取消”括号数字；`待处理 + 已取消 == 目标数` 才放行；已取消 > 0 时发 `segment_warning` 黄字提示（订单被临时取消不跳段）；不一致或无数字则停止该段；
   - 表头全选框（`:visible` 唯一）→ 全选 → “生成波次” → “按勾选数据”；
   - `_ensure_sort_rules`：读现有规则行标签，仅当缺失/不一致时修正（目标：第 1 条 库位、第 2 条 SKU*数量；实测默认已带）；
   - 点“确定”（**生产提交点，每段仅一次**；确认按钮用 `:visible` 限定，防止命中堆叠隐藏弹窗）；
   - `_extract_wave_no`：轮询结果弹窗文本，正则取 `W+13位`；成功就 `_close_visible_dialogs` 收尾（防止残留弹窗遮挡下一段）；
   - 成功即发 `segment_wave` 事件（前端逐段变绿、后端立即持久化）；
3. 段级异常（含 Playwright 超时等任意异常）不中断整批：记失败 → 恢复页面（重进目标页+页签+页大小）→ 继续下一段；恢复也失败才整体失败；
4. 结束汇总 `generated/failed`，消息区分“完成/部分完成”。

**WmsExportAutomation**：打开订单页 → 等数据 → 校验“渠道拆分”模板字段 → 触发导出下载 → 存到 downloads 目录 → 返回 `task_filename`（前端自动导入 `/api/jobs/{id}/file`）。

**WmsWavePrintAutomation**：按波次号进入波次页逐个生成拣货单 PDF，用 `QpdfMerger`（qpdf CLI）合并为 `Paper合并_日期.pdf`，供 `/api/jobs/{id}/merged` 下载。

**WmsWavePickAutomation**：波次页“待拣货”页签 → 分页快照所有波次（或按给定波次号）→ 逐波次拣货（含截单/确认弹窗处理）。

### 4.5 波次记录持久化（services/wave_records.py）

- 文件：`data/wave-records.json`，结构 `{"records":[{channel, seg_name, wave_no, order_count, at}]}`；
- `upsert_records`：按 `(channel, seg_name)` 去重、最新覆盖；`read_records`：容错（缺失/坏 JSON 返回 []）；`clear_records`：清空；
- 语义：**波次历史不跨批次保留**——前端导入新订单文件时调 `/api/wave-records/clear`；参数调整也清空。

---

## 5. 前后端联动协议

### 5.1 任务提交与轮询

前端 `wmsSubmit(mode)` → `POST /api/jobs`（`browser_mode` 来自浏览器模式开关，`confirm_production: true`，生成波次附 `segments: [{channel, seg_name, order_nos}]`，按导出顺序）→ `wmsStartPolling(job)` 每 1 秒 `GET /api/jobs/{id}`，用 `wms.events` 游标增量渲染日志。

### 5.2 事件流（events[].stage 语义，前端按 stage 渲染颜色/行为）

| stage | 含义 |
| --- | --- |
| `queued / launching / waiting_login` | 入队、启动浏览器、等页面就绪 |
| `searching` | 每段开始（含段序号与单数） |
| `segment_wave` | **message 为 JSON** `{"channel","seg_name","wave_no"}`：前端立即回填波次号并变绿，后端立即持久化 |
| `segment_warning` | 黄字：如“有 N 个订单已取消…继续生成波次” |
| `segment_failed` | 该段失败，恢复页面后继续下一段 |
| `awaiting_confirm / confirmed / observing` | 仅人工逐步确认模式（默认关闭） |
| `completed / completed_with_warnings` | 整批完成 / 部分完成 |
| `failed / cancelled` | 整体失败 / 取消 |

任务终态时：生成波次 → `wmsApplyWaveNos(job)` 回填结果中的波次号，成功或部分完成且至少一个波次号时 `wmsAutoExport()`（仅此一次，避免重复链接）；导出 → `wmsAutoImport(job)` 自动导入订单文件。

### 5.3 波次记录同步（跨页面/刷新恢复）

`wmsSyncWaveRecords()`（5 秒定时 + 服务恢复在线时）→ `GET /api/wave-records` → 对当前分析中存在的 `channel|seg_name` 回填/更新波次号并变绿（不触发导出）；`clearPending` 标记保证“导入后清理生效前”不误回填。

### 5.4 导出链接

`wmsAutoExport` → `POST /api/exports`（body 为 `wmsBuildExportBuffer()` 的 base64 → Uint8Array，xlsx 字节）→ 成功后 `wmsRenderExportLinks()` 从 `GET /api/exports`（最近 1 个）补挂下载链接；`linkedExports` Set 去重，刷新页面后也能恢复。

---

## 6. 已知陷阱（修改代码前必读）

1. **初始化顺序**：`app.js` 顶层初始化代码同步执行时，`var wms = {...}` 尚未赋值（var 提升但未初始化）；凡在初始化段同步访问 `wms.*` 的函数（如曾经的 `wmsRenderBrowserMode()`、健康循环）都会抛 TypeError 导致整页白屏。新代码一律经事件回调（异步）或保证在 `wms` 定义之后访问。
2. **虚拟滚动表**：WMS 订单表只渲染可见窗口行；**不要**试图用固定次数的滚动去“全量扫描”结果行（历史 bug：>319 单误报漏单）。生成波次已改为只核对子页签数量（待处理+已取消=目标数）。
3. **内存任务**：JobManager 全内存，重启服务 = 任务丢失 + 前端 404 → 前端会自动停止轮询并恢复空闲（提示刷新）。**运行中不要重启服务**。
4. **弹窗堆叠**：WMS 页面上隐藏的 el-dialog 常驻 DOM；所有点击类定位器必须带 `:visible` 限定，否则会命中不可见元素。
5. **CSS 变量与写死色**：表格底版等少数颜色是写死的（`.pt` 等），新主题必须在 `body[data-theme=...]` 里同时覆盖这些写死色。
6. **时间戳时区**：WMS 波次号日期按北京时间；本地时间戳可能跨天（如 17 日 18:00 本地 = 18 日 06:00 北京），文件命名用的是本地日期。
7. **config 选择器字段**：`automation.json` 的字段必须与 `config.py` 的 pydantic 模型一致，改一处要同步另一处；测试用真实选择器字符串键控 mock。
8. **测试端口**：`test-wms-ui.js` 会占用 8000 端口起 mock 服务，运行前需停掉真实后端。

---

## 7. 安全与生产不变量

- 生产操作必须 `confirm_production: true`；同一时刻仅一个活动任务；网络错误不重试生产提交；
- 生成波次：每个分段只点一次“确定”；搜索结果核对（待处理+已取消=目标数）通过才全选；任何歧义立即停止该段并保留现场信息；
- 出库单号/波次号白名单 `[A-Za-z0-9_-]+`；导出/下载接口带目录白名单；
- 服务只监听 127.0.0.1；浏览器 profile 属敏感数据，禁止打包外发。

---

## 8. 测试体系

### 8.1 规划页（Node.js + Chrome CDP，`planner/`）

- `test-helpers.js`：路径工厂（`paths(__dirname)` → html/sample/vendor/tmp）、Chrome 启动（headless=new）、CDP 驱动（send/evalJs/poll）、`importFileExpr`（base64 注入样本文件）、截图；
- `mock-wms-server.js`：8000 端口假后端（health/jobs/exports/wave-records + 测试控制面 `/api/__test/*`），供 `test-wms-ui.js` 全链路验证前端 WMS 交互；
- 套件：
  - `test-core.js`：core.js 纯函数单元测试（分区算法 vs 暴力 DP 等）；
  - `test-consistency.js`：参数矩阵 × 吸收档 × 归并 × 随机勾选的导出内容一致性模糊测试；
  - `test-e2e.js`：页面端到端（空状态骨架、配置主题、拖动排序、几何稳定等）；
  - `test-web-chain.js`：构建产物与源码链路一致性；
  - `test-wms-ui.js`：WMS 联动 UI 全流程（需 8000 端口空闲）；
  - `test-invariants.js`：订单分配/导出不变量（5700 单样本 + 合成多行/未知类型用例 + 主题切换不影响状态）。

### 8.2 后端（pytest，76 项，`wms/project/tests/`）

```bash
cd wms/project
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=backend .venv/bin/python -m pytest -p no:cacheprovider -q
```

- `test_config.py`：配置模型校验；`test_api.py`：接口契约（确认开关、白名单、导出目录）；`test_job_manager.py`：任务生命周期（成功/部分/取消/并发/逐段持久化）；`test_wave_generate.py`：生成波次自动化各步骤（fake page 按真实选择器字符串键控，覆盖：页大小跳过、总数核对与取消订单兜底、段级失败继续、排序规则、弹窗关闭、残留结果自动重查）；`test_wave_pick/print/export/pdf_centering/wave_records`。

---

## 9. 扩展指南

- **新增页面主题**：part1-body 的 `#themeList` 加 `<li data-v="key">名字</li>`；part0-head 加 `body[data-theme="key"]{--bg:...}` 及写死色覆盖（`.pt`/`.pt th`/`.bb:hover`/`.wms-mode.sel`）；e2e 更新主题数量与颜色断言。
- **新增 WMS 工作流**：config.py 加子配置 → automation.json 加选择器 → 新建 automation 模块（返回 dataclass result）→ job_manager `RunMode`/`_execute` 加分支 → routes 校验请求体 → 前端加功能按钮与轮询后的结果应用。
- **新增导出工作表**：core.js 写 `sheetXxx` 返回 `{name, aoa, merges, styles, cols}`，在 `buildExport` 的 sheets 数组追加；注意 sheet 名走 `sanitizeSheetName`。
- **调整 WMS 页面选择器**：只改 `config/automation.json`（页面改版时通常只需动选择器），选择器必须唯一可辨（页面同类元素多，用 `:has(text)` 锚定）。

---

## 10. 常用命令速查

```bash
# 构建/部署前端
cd planner && python3 build.py

# 安装后端依赖
cd wms/project && ./scripts/setup.sh

# 启动服务（127.0.0.1:8000）
cd wms/project && ./scripts/start.sh          # 或 ./scripts/start.sh 9000

# 健康检查
curl http://127.0.0.1:8000/api/health

# 后端测试
cd wms/project && PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=backend \
  .venv/bin/python -m pytest -p no:cacheprovider -q

# 规划页全部测试（test-wms-ui 前需停后端释放 8000）
cd planner
node test-core.js && node test-consistency.js
node test-e2e.js && node test-web-chain.js && node test-invariants.js
node test-wms-ui.js

# 自主全流程驱动（有头浏览器，导入 sample 订单跑生成波次）
cd wms/project && .venv/bin/python scripts/run_full_auto.py
```
